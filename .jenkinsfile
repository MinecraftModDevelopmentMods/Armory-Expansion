pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git pull
                echo 'Checking out branch..'
            }
        }
        stage('Build') {
            steps {
                gradlew clean build
                echo 'Building branch..'
            }
        }
    }
}