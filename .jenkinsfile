pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git pull
                echo 'Checking out branch..'
            }
        }
        stage('Build') {
            steps {
                sh 'gradle clean build'
                echo 'Building branch..'
            }
        }
    }
}