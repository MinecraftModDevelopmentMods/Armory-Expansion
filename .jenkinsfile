pipeline {
    agent any

    stages {
        stage('Checkout') {
            git pull
            echo 'Checking out branch..'
        }
        stage('Build') {
            steps {
                gradlew clean build
                echo 'Building branch..'
            }
        }
    }
}